---
type Props = {
  eyebrow?: string;
  title: string;
  description?: string;
  price: number;
  priceNote?: string;
  badge?: string;
  featured?: boolean;
  features?: string[];
  buttonLabel: string;
  href: string;
  trackingId?: string;
};

const {
  eyebrow,
  title,
  description,
  price,
  priceNote = "one-time enrollment",
  badge,
  featured = false,
  features = [],
  buttonLabel,
  href,
  trackingId,
} = Astro.props;
---

<article class:list={["package-card", { featured }]}>
  {badge && <span class="package-badge">{badge}</span>}
  {eyebrow && <p class="package-eyebrow">{eyebrow}</p>}
  <h3>{title}</h3>
  {description && <p class="package-description">{description}</p>}
  <p class="package-price" aria-label={`$${price}`}>
    <span aria-hidden="true">$</span>{price}
  </p>
  <p class="package-price-note">{priceNote}</p>
  <ul>
    {features.map((feature) => <li>{feature}</li>)}
  </ul>
  <a
    class="button button-primary package-button"
    href={href}
    target="_top"
    data-product={trackingId}
  >
    {buttonLabel}
  </a>
</article>
